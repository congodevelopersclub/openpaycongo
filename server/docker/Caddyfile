{
    admin off
    auto_https off

    servers {
        trusted_proxies static private_ranges
        trusted_proxies_strict
    }
}

:8080 {
    root * /var/www/html/public
    encode zstd gzip
    php_server
}
